# Senf

Senf is a secure backend service for managing encrypted environment files with SSH key-based authentication. It allows users to store, retrieve, and update `.env` files with end-to-end encryption, enabling secure sharing and version control of sensitive configuration data. Multiple SSH keys per user are supported for flexible access management.

For more information, visit the [documentation](https://senf-docs.kaenguruu.dev).
